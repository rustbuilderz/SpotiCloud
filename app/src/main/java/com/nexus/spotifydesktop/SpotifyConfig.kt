package com.nexus.spotifydesktop

object SpotifyConfig {
    // Must match Dashboard Redirect URI and MainActivity intent-filter (lowercase)
    const val REDIRECT_URI = "com.nexus.spotifydesktop://callback"
}
